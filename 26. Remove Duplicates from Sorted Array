//https://leetcode.com/problems/remove-duplicates-from-sorted-array/
class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        
        auto ind = unique(nums.begin(), nums.end());
        int n= ind - nums.begin(); 

        return n;
    }
};
